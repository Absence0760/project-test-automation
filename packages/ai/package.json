{
  "name": "@bettertest/ai",
  "version": "0.1.0",
  "description": "AI layer — local LLM (Ollama) + optional cloud for step inference and test generation",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.11"
  }
}
